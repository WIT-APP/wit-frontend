import { useMutation, useQueryClient } from "@tanstack/react-query"; 
import { Applicant } from "../interfaces/applicant.interface";


const useRegisterApplicant = () => {

    const newApplication = async (applicant: Applicant) => {
        
    }
}